"use strict";

var assert = require("assert"),
  path = require('path');

describe("Arin", function () {
  var arin = require('./../lib/arin.js');

  it("should return path to index.scss", function(){
    var expected = path.join(__dirname, '../scss', "index.scss");
    (arin.getCoreSrc()).should.be.equal(expected);
  });

  /*it("should return an array of dependencies", function(){
    var modules = arin.getModules();
    //console.log(modules);
    (modules.length).should.be.above(0);
  });*/

});